<div class="stat-card">
    <div>
        <h4>{{localize (concat "sr3d.actor.karma." karma)}}</h4>
    </div>
    <div class="stat-label">
        <div class="stat-value">
            <h1>
                {{data.total}}

                {{#if data.mod}}
                / {{data.mod}}
                {{/if}}
            </h1>
        </div>
    </div>
</div>