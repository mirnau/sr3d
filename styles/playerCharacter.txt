form {
    align-self: flex-start; /* Keeps the form itself aligned at the top */
    width: 100%; /* Optional: If you want it to take full width */
    min-height: 600px;
  }

/* Base styling for grid container */
.grid-container {
    gap: 10px;
    padding: 10px;
    align-items: start;
}

.content-section {
    min-height: 400;
}

.one-column {
    display: grid;
    grid-template-columns: 1fr;
  }
  
  .two-column {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
  }
  
  .four-column {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }
  

/* Style individual sections to avoid overlapping */
.grid-item {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    min-height: 200px;
    min-width:200px; 
}

/* Ensure .grid-item elements respond well in all grid layouts */
.grid-container.one-column .grid-item,
.grid-container.two-column .grid-item,
.grid-container.four-column .grid-item {
    grid-column: span 1;
}
